import AuthContent from "../../components/Auth/AuthContent"

const PagePreview = (id) => {
    return <AuthContent>{'Page preview'}</AuthContent>
    // return'Page preview'
}
export default PagePreview